  The target environment already has the transfer package from this morning. It has sud-questions.yaml with 10 "BOS 
  KA01"/"BOS KA02" references in prior_art fields. These are internal O-BOK codes that mean nothing to an LLM. The
  fix:

  In onboard/sud-questions.yaml, find and replace these 10 entries:

  1. "BOS KA01 tier classification" → remove (ISO 22301 and IEC 60812 entries already present cover it)
  2. "BOS KA01 priority classification" → replace with "SRE forced prioritization — CRITICAL/HIGH/MEDIUM with
  behavioral definitions"
  3. "BOS KA01 four-category model" → remove (ISO 22301 BIA impact categories already present covers it)
  4. "BOS KA01 layer ownership model" → replace with "SRE three-actor ownership model — ownership varies by
  observability layer"
  5. "BOS KA01 four-layer model" → replace with "Four-layer observability model — System/Process/Business
  Health/Business Impact"
  6. "BOS KA02 layer classification" → replace with "Signal layer taxonomy — layer determines ownership, semantics,
  and consumer"
  7. "BOS KA02 causal model between layers" → replace with "Distributed tracing causal model (Ben Sigelman) —
  cross-layer diagnostic path"
  8. "BOS KA01 impact quantification" → remove (ISO 22301 and SLODLC entries already present cover it)
  9. "BOS KA01" (bare, in q28) → remove (ISO 22301 BIA impact categories already present covers it)
  10. "BOS KA02 coverage completeness" → replace with "Signal coverage completeness — identified capability without
  signal = blind spot"

  In onboard/generate_docs.py line 32: change "SRE SLO + BOS KA02: What signals, at what layers?" to "SRE SLO + Signal
   Design: What signals, at what layers?"

  Then regenerate: python onboard/generate_docs.py and python onboard/render_instrument.py -o
  onboard/sud-instrument.html
